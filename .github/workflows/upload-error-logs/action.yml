name: 'Upload Error Logs'
description: 'Reusable workflow for uploading error logs from failed workflows'
runs:
  using: "composite"
  steps:
    - name: Upload Error Logs
      shell: bash
      run: |
        echo "This is a reusable workflow. Use it with:"
        echo "uses: ./.github/workflows/upload-error-logs.yml"