name: 'Change Stage and Deploy'
description: 'Reusable workflow for changing stage and deploying documentation'
runs:
  using: "composite"
  steps:
    - name: Change Stage and Deploy
      shell: bash
      run: |
        echo "This is a reusable workflow. Use it with:"
        echo "uses: ./.github/workflows/change-stage.yml"